using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//On tile object is an object that is placed on a tile (like a wall but its not a wall)
public class OnTileObj : MonoBehaviour
{
    public int x, y; //Position of object on tile
    // Start is called before the first frame update
    public int size = 1 ; //Size of object (how many tiles it takes. 2 equals 4 tiles taken)
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
